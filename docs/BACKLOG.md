# Бэклог

Накопленные задачи которые сознательно отложены на потом. Сюда же кладём
наблюдения с пилота которые требуют отдельной работы.

## Парсинг цен (разнобой у поставщиков)

**Проблема.** В прайсах поставщиков колонка с ценой называется по-разному
и часто не совпадает с текущими синонимами PRICE:
- TEL (Техэлектро): цена не определилась — заголовок неизвестен (нужно увидеть файл)
- Может встречаться: `РРЦ`, `Цена розничная`, `Розн. цена`, `Цена реализации`,
  `Отпускная цена`, `Прайс`, `Цена прайс`, `Цена опт.`, `Стоимость единицы`,
  `Цена с НДС` vs `Цена без НДС` (две колонки — какую брать?)
- Иногда цена в формате с символом валюты (`100 ₽`), форматирование `1 234,56`
- Может быть несколько ценовых колонок (опт/розн/акция) — нужен выбор

**Что делать.**
1. Собрать примеры заголовков из всех загруженных прайсов (`TEL`, `INT`, `AKR`...)
2. Расширить `COLUMN_SYNONYMS[SpecField.PRICE]`
3. Если есть несколько ценовых колонок — продумать UX выбора (через UI «Особенности прайса» — `preferred_price_column: "розничная"`)
4. Парсер для строк `"100 ₽"` уже частично работает (parse_decimal), но проверить кейсы с пробелами как разделителями тысяч

Текущие синонимы PRICE:
```
"цена", "стоимость", "цена за единицу", "цена за ед",
"price", "unit price", "cost"
```

## Прочее в бэклоге

### Инфраструктура
- ~~**External backup (S3/scp/rsync)**~~ ✓ done 2026-06-01: ежедневный rclone → Google Drive (см. `deploy/OPERATIONS.md`)
- **Bind-mount postgres volume** — превентивная защита от случайного `docker volume prune`
- **Systemd watchdog для авто-recovery** — пока инцидентов нет, но было бы хорошо иметь
- ~~**Cleanup deactivated**~~ ✓ done 2026-06-02: ежедневный cron в 04:20 UTC удаляет «осиротевшие» (без ссылок) deactivated позиции

### Парсер / Импорт
- **Несколько ценовых колонок** — см. выше
- **TEL артикулы** — 151 позиция (5970-5819) без артикула, проверить чем они отличаются
- **Цена с разделителями** — некоторые прайсы используют `1 234,56` или `1,234.56`, проверить parse_decimal
- **Поддержка PDF/DOCX** — Phase 2

### UI
- **Picker каталог-карточки** для manual override catalog-link (сейчас только через API PATCH)
- **«Особенности прайса» — preview** эффекта трансформаций на пример строки
- **Bulk-операции** над поставщиками (массовый пере-импорт, экспорт настроек)
- **Страница каталога** с поиском/фильтрами (сейчас только импорт)

### Матчинг
- **Тонкая настройка весов** в reranker (`w_article=0.5, w_lexical=0.5, boost_brand=0.1, boost_unit=0.05`) — должно подстраиваться под gold dataset когда заказчик его заполнит
- **Semantic search через pgvector** — Phase 2
- **Атрибуты товара** (Phase 2) — сейчас `attributes={}` всегда

### Тесты
- Один pre-existing fail `test_e2e_eval_run_with_real_template_file` — индексы колонок устарели после обновления `gold_dataset_template_new.xlsx`. Поправить когда возьмёмся за gold dataset.

## Не делать (YAGNI)

- **Конкатенация колонок** для `name` (как было нужно для TEL) — один раз правится в Excel, не стоит code complexity. Документировано в `deploy/OPERATIONS.md`.
- **Per-supplier custom parsers** — то же. Только общие синонимы + Особенности прайса.
