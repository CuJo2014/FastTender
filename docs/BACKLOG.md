# Бэклог

Накопленные задачи которые сознательно отложены на потом. Сюда же кладём
наблюдения с пилота которые требуют отдельной работы.

## Парсинг цен (разнобой у поставщиков) — ✓ done 2026-06-03

Закрыто эпиком `item.prices` (шаги 1–4) + детектором ценовых колонок
`services/parser/price_columns.py` (`b64e98d`):
- ~~Несколько ценовых колонок / выбор нужной~~ ✓ детектор всех колонок:
  net/gross (база НДС), `РРЦ`, `МИЦ`, розница/опт/акция; `select_preferred()`
  выбирает цену (per-supplier правила подтверждены 2026-06-02).
- ~~Хранение нескольких цен~~ ✓ `Item.prices` JSONB + миграция 0010, реимпорт
  по `header_row`, дефолт ставки НДС.
- ~~TEL: заголовок «неизвестен»~~ ✓ разобраны пары «с НДС / без НДС», `РРЦ`, `МИЦ`.

Осталось проверить (мелочь, не блокер):
- **parse_decimal на US-формате** `1,234.56` — `1 234,5` и десятичная запятая
  уже поддержаны, US-формат с запятой-разделителем тысяч проверить отдельно.
- Под-задача «расширить `COLUMN_SYNONYMS[PRICE]`» **снята** — её заменил модуль
  `price_columns` (список синонимов в `header_detector` править не нужно).

## Прочее в бэклоге

### Инфраструктура
- ~~**External backup (S3/scp/rsync)**~~ ✓ done 2026-06-01: ежедневный rclone → Google Drive (см. `deploy/OPERATIONS.md`)
- **Bind-mount postgres volume** — превентивная защита от случайного `docker volume prune`
- **Systemd watchdog для авто-recovery** — пока инцидентов нет, но было бы хорошо иметь
- ~~**Очистка `frontend_dist` при деплое**~~ ✓ done 2026-06-05: `frontend/Dockerfile` чистит том (`find /srv/frontend -mindepth 1 -delete`) перед копированием нового dist — старые хешированные бандлы больше не копятся.
- ~~**Cleanup deactivated**~~ ✓ done 2026-06-02: ежедневный cron в 04:20 UTC удаляет «осиротевшие» (без ссылок) deactivated позиции

### Парсер / Импорт
- ~~**Несколько ценовых колонок**~~ ✓ done 2026-06-03 (`b64e98d`, `price_columns.py`) — см. раздел «Парсинг цен» выше
- ~~**TEL артикулы**~~ ✓ done 2026-06-04: «Код» в прайсе поставщика = их артикул (ARTICLE) (`2224ce3`). NB: убедиться что 151 позиция (5970-5819) теперь получила артикул при ре-импорте
- ~~**Цена с разделителями**~~ ✓ done: `parse_decimal` поддерживает `1 234,5` и десятичную запятую (проверить только US-формат `1,234.56` — см. выше)
- **Поддержка PDF/DOCX** — Phase 2

### UI
- **Picker каталог-карточки** для manual override catalog-link — пока НЕ сделано (ни API, ни UI-пикера; есть только enum `catalog_link_source: auto|manual` и индикация в `CandidatesTable`)
- **«Особенности прайса» — preview** эффекта трансформаций на пример строки
- **Bulk-операции** над поставщиками (массовый пере-импорт, экспорт настроек)
- **Страница каталога** с поиском/фильтрами — частично: `CatalogPage.tsx` есть, но показывает только статистику/импорт; **поиск + фильтры открыты**
- **Вариант `Button` для «опасных» ghost-действий** — у `ghost` цвет текста (`text-slate-700`) перебивает кастомный `className` (равная специфичность, побеждает порядок в собранном CSS). Сейчас «Удалить» в Спецификациях держит цвет через `!text-pink-300` (костыль). Завести вариант `danger-ghost` (розовый/красный текст) в `components/ui/Button.tsx`, чтобы цвет задавался компонентом без `!important`.
- **Изменяемая ширина колонок таблицы строк спеки** (запрошено 2026-06-05) — на странице деталей спеки (`SpecificationDetailPage.tsx`) дать пользователю тянуть границы колонок (drag-resize) и **сохранять ширины глобально** в `localStorage` (ключ `spec.columnWidths`, одна раскладка на все спеки), восстанавливать при открытии. Дизайн: `table-layout: fixed` + `<colgroup>`, drag-handle на правом крае каждого `<th>`, новый хук `frontend/src/hooks/useColumnWidths.ts` (по образцу localStorage-паттерна из `SpecificationsListPage.tsx:10-27`), reset по double-click и кнопкой «сбросить ширины». ВАЖНО: не оборачивать таблицу в `overflow-x-auto` — сломает sticky-заголовки (`top` offset); пусть таблица расширяет страницу (горизонтальный скролл на уровне body). Inner `CandidatesTable` не трогать. Без новых npm-зависимостей.

#### ~~Форма спецификации (детали) — запрошено заказчиком 2026-06-04~~ ✓ done 2026-06-04
Все три пункта реализованы и выкачены на прод (`6012fd3`, миграции 0011-0013):
1. ~~**«Клиент» как отдельная сущность (справочник)**~~ ✓ таблица `Client` +
   FK `specification.client_id`, CRUD `/clients`, справочник «Клиенты», picker
   на форме (коммиты «Клиенты шаг 1/2/3»).
2. ~~**«Отказаться от сопоставления» (undo верификации строки)**~~ ✓ reset-эндпоинт
   + кнопка в строке (`afd1ce5`).
3. ~~**Реквизиты спецификации** (торг. площадка, номер, дата, дата поставки)~~ ✓
   поля + блок на форме + экспорт; справочник торговых площадок (`d69b7f4`, `6012fd3`).

### Матчинг
- **Тонкая настройка весов** в reranker (`w_article=0.5, w_lexical=0.5, boost_brand=0.1, boost_unit=0.05`) — должно подстраиваться под gold dataset когда заказчик его заполнит
- **Semantic search через pgvector** — Phase 2 (`PgVectorRepository` — заглушка)
- **Атрибуты товара** (Phase 2) — сейчас `attributes={}` всегда

### Тесты
- ~~Pre-existing fail `test_e2e_eval_run_with_real_template_file`~~ ✓ обновлён под новый шаблон `gold_dataset_template_new.xlsx` (двухуровневая шапка, 17 колонок с code_1c: col 14 = «артикул матчера», col 17 = «совпало?»). NB: подтвердить зелёным прогоном.

## Не делать (YAGNI)

- **Конкатенация колонок** для `name` (как было нужно для TEL) — один раз правится в Excel, не стоит code complexity. Документировано в `deploy/OPERATIONS.md`.
- **Per-supplier custom parsers** — то же. Только общие синонимы + Особенности прайса.
